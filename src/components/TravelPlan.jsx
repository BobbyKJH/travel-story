// 여행 계획
import React, { useState } from "react";
import styles from "./TravelPlan.module.css";

export default function Plan() {
  const [plan, setPlan] = useState([]);
  const [list, setList] = useState("");

  return <div>Plan</div>;
}

// 여행지 수기 작성
// 여행지 리스트 api들고와서 나라 리스트 뿌려주기
